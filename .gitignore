# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code 的本机配置（`.vscode/`）
#
# Flutter 模板默认把这行注释掉，理由是「你可能想共享调试配置」。
# 但本项目里的 settings.json 只有一条 `git.ignoreLimitWarning`
# —— 那是个人偏好（关掉一个警告），与项目无关。
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# ===== 崩溃与运行日志 =====
#
# 这两类文件都**不该入库**，而且是实测踩到的（开源审计时在仓库里发现过）：
#   - `hs_err_pid*.log`：JVM 崩溃转储，含本机完整路径、内存布局、
#     加载的 jar 列表 —— 等于把开发机的环境信息公布出去；
#   - `flutter_0*.log`：Flutter 崩溃报告，含调用栈与设备信息。
#
# `.gitignore` 顶部已有 `*.log`，但 `hs_err_pid...log` 在 android/ 子目录，
# 且这里再写一遍是为了**意图明确**：看到这几行就知道「日志不要提交」是
# 有意为之，而不是碰巧被 `*.log` 覆盖。
hs_err_pid*.log
flutter_*.log

# ===== 开发期工具（本地保留，不入库）=====
#
# `tools/` 里是开发时用的脚本：图标生成、验证码语料、帧耗时分析、
# 模型量化评估、开源前审计。
#
# 它们**不参与构建、也不影响使用者**（clone 下来照样能 `flutter build`），
# 属于开发者的工作工具而非项目本体 —— 就像不会把 IDE 配置一起开源。
# 因此整目录排除，本地文件保持可用。
#
# 注意：`git rm --cached` 只停止跟踪，文件仍在工作区；
# 但**已被跟踪过的文件不受 .gitignore 影响**，必须先 rm 掉索引记录。
/tools/

# 语料（tools/ocr_eval/）本就在这棵树下面，被上面那条覆盖。
# 它是从学校系统实际抓取的验证码图片（文件名前缀就是答案），
# 不含个人信息但属于第三方系统数据，公开发布不妥；
# `flutter test` 用它校验预处理尺寸时会自动跳过不存在的目录
# （见 test/captcha_pipeline_test.dart）。

# ===== 构建产物：打进仓库会让 clone 变成几百 MB（不进库）=====
#
# `release/` 是本地整理好的发布包（三个 ABI 加起来约 165 MB）。
# 二进制不该进版本库，理由很实际：
#   1. clone 体积 —— 每次历史都带着这几百 MB，且**永久留存在历史里**，
#      即使以后删掉文件也瘦不回来；
#   2. GitHub 单文件上限 100 MB，`all` 版 88 MB 已经很接近，
#      下一次功能更新就可能超限；
#   3. 发布包的正确去处是 Releases 页（附件不占仓库体积）。
#
# 需要包时本地执行：
#   flutter build apk --release --split-per-abi && flutter build apk --release
/release/
**/*.apk
